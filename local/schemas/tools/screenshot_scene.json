{
  "name": "screenshot_scene",
  "description_lines": [
    "Capture a PNG from a loadable PackedScene, including authored `.tscn`/`.scn` scenes and imported 3D sources such as GLB/GLTF.",
    "The public tool surface is intentionally small: pass `scene_path` and optionally exactly one of `code` or `script_path`. Camera paths, target paths, view rectangles, and other parameter-driven screenshot modes are not accepted.",
    "If code and script_path are both omitted, the tool captures `ctx.root` using deterministic automatic framing.",
    "Screenshot scripts must use `@tool extends RefCounted`, define `func run(ctx) -> void`, and call `ctx.capture(node_or_array, options)` exactly once. `ctx.root` is the detached scene root.",
    "Use normal Godot APIs to find nodes, construct arrays, temporarily change visibility or transforms, add markers, or create a Camera2D/Camera3D. The detached scene instance is rendered and then discarded; the authored scene is not saved.",
    "The complete context API is `ctx.root`, `ctx.capture(...)`, `ctx.log(...)`, and `ctx.error(...)`.",
    "Capture options support `context_margin`, 3D `view` (`front`, `back`, `left`, `right`, `top`, `perspective`, or `isometric`), and `camera` containing a Camera2D or Camera3D under ctx.root. Omit options for automatic framing.",
    "Inline code is saved under `res://.fennara/tmp/editor_scripts/`; the response returns the effective script_path. Patch and reuse that script_path on retries instead of resending large code.",
    "A successfully encoded screenshot is saved under user://.fennara/screenshots and returned as image_res_path plus a cross-platform absolute image_path.",
    "Image-capable clients receive the screenshot as transient model image context. Text receipts and saved paths remain available as fallback.",
    "Sparse 3D captures return the image with coverage/span metrics and a warning instead of hiding it."
  ],
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "scene_path": {
        "type": "string",
        "description": "Path to a loadable PackedScene, such as `res://scenes/level.tscn` or `res://assets/model.glb`."
      },
      "code": {
        "type": "string",
        "description": "Inline GDScript for the first procedural capture. Provide code or script_path, never both. Contract: `@tool extends RefCounted`, `func run(ctx) -> void`, and exactly one `ctx.capture(node_or_array, options)` call."
      },
      "script_path": {
        "type": "string",
        "description": "Existing `.gd` screenshot worker. Prefer the returned temp script_path for patch-and-rerun workflows. Provide code or script_path, never both."
      }
    },
    "required": ["scene_path"]
  }
}
